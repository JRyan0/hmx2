<div class="mb-2">
    <label for="{{ $name }}" class="form-label">{{ $label }}</label>
    <input type="{{ $type }}" placeholder="{{$placeholder}}" value="{{ $value }}" name="{{ $name }}" class="form-control" id="{{ $name }}">
</div>
